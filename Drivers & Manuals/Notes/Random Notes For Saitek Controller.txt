1) Create a default state class/enumeration that gets mapped to a button. When this button is pressed the rover returns to this default state. For example, a default state for the arm would keep it up and out of the way while we drive and give us a known starting point for everything else.

2) 